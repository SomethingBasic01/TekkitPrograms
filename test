-- Function to move items from the dispenser below to any chest on the network
local function moveItemsToChest()
    local dispenserName = "bottom"  -- Assuming the dispenser is directly below the turtle
    local chestName = "right"  -- Assuming the chest is on the right side of the turtle or adjust as necessary
    
    -- Pull all items from the dispenser below
    for slot = 1, 16 do
        local pulled = turtle.suckDown()  -- Pull items from the dispenser into the turtle's inventory
        if pulled then
            print("Pulled item from dispenser.")
            -- Now push the item into the connected chest
            turtle.push(chestName)  -- Push items into the chest on the network
        end
    end
end

-- Run the function
moveItemsToChest()
